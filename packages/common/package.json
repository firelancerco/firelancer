{
    "name": "@firelancerco/common",
    "version": "1.3.0-alpha.2",
    "main": "index.js",
    "files": [
        "lib/**/*"
    ],
    "scripts": {
        "watch": "tsc -p ./tsconfig.build.json -w",
        "build": "rimraf lib && tsc -p ./tsconfig.build.json",
        "lint": "eslint --fix .",
        "ci": "npm run build"
    },
    "publishConfig": {
        "registry": "https://npm.pkg.github.com/",
        "access": "public"
    },
    "devDependencies": {
        "rimraf": "^6.0.1",
        "typescript": "^5.7.2"
    },
    "dependencies": {
        "rxjs": "^7.8.2"
    }
}
